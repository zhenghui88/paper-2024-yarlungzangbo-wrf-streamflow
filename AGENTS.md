# Agent Configuration

## Project Overview
This is a LaTeX project for writing academic papers. The main file is `main.tex`, and it includes several sections such as introduction, methodology, results, and conclusion. There is a suplementary information file `si.tex` for additional content. The project also includes a bibliography file `references.bib` and `latexmkrc` for build configuration.
